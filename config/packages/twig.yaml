twig:
    default_path: '%kernel.project_dir%/templates'
    form_themes: ['_composants/form/forms.html.twig']
    paths:
        # point this wherever your images live
        '%kernel.project_dir%/assets/images': images

when@test:
    twig:
        strict_variables: true
