{% extends 'baseDash.html.twig' %}

{% block title %}Edit Prestation{% endblock %}

{% block body %}
    <h1 class="text-xl font-semibold text-gray-900 sm:text-2xl dark:text-white">Edit Prestation</h1>

    {{ include('prestation/_form.html.twig', {'button_label': 'Update'}) }}
    <a href="{{ path('app_prestation_index') }}">back to list</a>
    {{ include('prestation/_delete_form.html.twig') }}
{% endblock %}
