<form
  method="post"
  action="{{ path('app_customer_delete', { id: customer.id }) }}"
  onsubmit="return confirm('Are you sure you want to Supprimer this item?');"
>
  <input
    type="hidden"
    name="_token"
    value="{{ csrf_token('Supprimer' ~ customer.id) }}"
  />
  <button
    class="btn bg-red-700 hover:bg-red-800 text-white font-medium rounded-lg text-sm px-5 py-2.5 focus:outline-none focus:ring-4 focus:ring-red-300 dark:bg-red-600 dark:hover:bg-red-700 dark:focus:ring-red-900"
  >
    Supprimer
  </button>
</form>
