{% extends 'base.html.twig' %}

{% block title %}Company{% endblock %}

{% block body %}
    <h1>Company</h1>

    <table class="table">
{#        <thead>#}
{#        <tr>#}
{#            <th>Nom de l'association</th>#}
{#            <th>Description</th>#}
{#            <th>Date de création</th>#}
{#            <th>Actions</th>#}
{#        </tr>#}
{#        </thead>#}
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ company.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ company.logo }}</td>
            </tr>
            <tr>
                <th>TVA</th>
                <td>{{ company.TVA }}</td>
            </tr>
            <tr>
                <th>License_validity</th>
                <td>{{ company.licenseValidity ? company.licenseValidity|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Id_license</th>
                <td>{{ company.idLicense }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_company_index') }}">back to list</a>

    <a href="{{ path('app_company_edit', {'id': company.id}) }}">edit</a>

    {{ include('company/_delete_form.html.twig') }}
{% endblock %}
